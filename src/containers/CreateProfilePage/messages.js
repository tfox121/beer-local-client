/*
 * CreateProfilePage Messages
 *
 * This contains all the text for the CreateProfilePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CreateProfilePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CreateProfilePage container!',
  },
});
