// import { selectProducerProfilePageDomain } from '../selectors';

describe('selectProducerProfilePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(true);
  });
});
